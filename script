killall gpg-agent
rm -rf /etc/pacman.d/gnupg/
pacman-key --init
pacman-key --populate
pacman -Sy archlinux-keyring --noconfirm
pacman -S vim linux-headers base-devel reflector networkmanager git xdg-utils xdg-user-dirs bash-completion ntfs-3g os-prober --noconfirm --needed
systemctl enable NetworkManager
while true
    do
    read -rp "Do you want to pick up pacman mirror-servers or use the default ones? [y/n] " yn
    case $yn in
        [Yy]* ) 
        read -rp "Specify country name: " srvloc
        while ! reflector --country "$srvloc" --save /etc/pacman.d/mirrorlist; do
        echo "Invalid country. Tip: it has to be english version of the country name"
        read -rp "Specify country name: " srvloc
        done
        ;;
        [Nn]* ) 
        break
        ;;
        * ) echo "Invalid option";;
    esac
    done
read -rp "Specify region (e.g. Europe/Prague): " localization
ln -sf /usr/share/zoneinfo/"$localization" /etc/localtime
hwclock --systohc
locales=$(ls /usr/share/i18n/locales)
PS3="Select language: "
options=("$locales")
select opt in "${options[@]}"
do
        *)
    echo "Invalid option"
    ;;
esac
done

while true
    do
    read -rp "Arch was installed successfully. Do you want to exit chroot? [y/n] " yn
    case $yn in
        [Yy]* ) rm script && exit;;
        [Nn]* ) break;;
        * ) echo "Invalid option";;
    esac
    done
